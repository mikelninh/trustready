# Roadmap visual labels

`.harness/roadmap.json` may include an optional short `label` beside a full milestone `title`.

The title remains the explanatory roadmap truth. The label is only a compact UI representation for Mission Control and must preserve the same meaning.
