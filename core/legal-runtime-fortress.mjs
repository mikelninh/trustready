import crypto from 'node:crypto'
import {ZONES,authorizeMatter,parseTime,sha256,signEnvelope,verifyEnvelope,verifyIdentityAssertion} from './legal-key-identity.mjs'
import {inspectPayload} from './legal-dlp.mjs'
import {evaluateNetworkEgress,verifyEgressEnforcement} from './legal-network.mjs'

const HIGH_RISK_ACTIONS=new Set(['send_email','bea_send','case_write','deadline_write','delete_document'])
function deny(code,reason,now=new Date(),extra={}){return{allowed:false,code,reason,decided_at:now.toISOString(),...extra}}

export function signProviderPassport({body,private_key,key_id}){return signEnvelope({body,private_key,key_id,purpose:'provider_review'})}
export function verifyProviderPassport({passport,key_store,now=new Date()}){const v=verifyEnvelope({envelope:passport,key_store,purpose:'provider_review',now});if(!v.valid)return v;const b=v.body;if(b.status!=='approved'||parseTime(b.valid_until)<=now.getTime())return{valid:false,reason:'provider not currently approved'};if(b.training_on_customer_data!==false||!b.use_cases||typeof b.third_country!=='boolean')return{valid:false,reason:'provider training/use-case/transfer classification invalid'};if(b.third_country===true&&b.transfer_safeguards_status!=='approved')return{valid:false,reason:'third-country safeguards missing'};return{valid:true,body:b,signer_key_id:v.signer_key_id}}
export function evaluateRuntimeState({state,tenant_id,provider_id,action=false}){if(!state||state.external_ai_enabled!==true)return{allowed:false,reason:'external AI kill switch is off'};if(state.disabled_tenants?.includes(tenant_id))return{allowed:false,reason:'tenant disabled'};if(state.disabled_providers?.includes(provider_id))return{allowed:false,reason:'provider disabled'};if(action&&state.outbound_actions_enabled!==true)return{allowed:false,reason:'outbound action kill switch is off'};return{allowed:true}}

export function authorizeLegalEgress({identity_assertion,request,provider_passport,key_store,runtime_state,network_probe,egress_enforcement_attestation,now=new Date()}){if(!request||!(request.zone in ZONES))return deny('INVALID_REQUEST','valid zone required',now);const state=evaluateRuntimeState({state:runtime_state,tenant_id:request.tenant_id,provider_id:request.provider_id});if(!state.allowed)return deny('KILL_SWITCH',state.reason,now);if(!request.policy_version||runtime_state.policy_version!==request.policy_version)return deny('POLICY_VERSION_DENIED','active policy version mismatch',now);const enforced=verifyEgressEnforcement({attestation:egress_enforcement_attestation,key_store,tenant_id:request.tenant_id,policy_version:request.policy_version,now});if(!enforced.valid)return deny('EGRESS_ENFORCEMENT_DENIED',enforced.reason,now);const identity=verifyIdentityAssertion({assertion:identity_assertion,key_store,now});if(!identity.valid)return deny('IDENTITY_DENIED',identity.reason,now);if(identity.principal.session_id!==request.actor_session_id)return deny('SESSION_MISMATCH','session mismatch',now);const matter=authorizeMatter({principal:identity.principal,tenant_id:request.tenant_id,matter_id:request.matter_id,operation:'egress',zone:request.zone});if(!matter.allowed)return deny('MATTER_DENIED',matter.reason,now);if(request.zone==='RESTRICTED')return deny('RESTRICTED_EXTERNAL_DENIED','Zone 4 external egress disabled by default',now);const providerV=verifyProviderPassport({passport:provider_passport,key_store,now});if(!providerV.valid)return deny('PROVIDER_DENIED',providerV.reason,now);const provider=providerV.body;if(provider.provider_id!==request.provider_id)return deny('PROVIDER_MISMATCH','provider mismatch',now);const policy=provider.use_cases?.[request.use_case];if(!policy||!policy.allowed_zones?.includes(request.zone))return deny('USE_CASE_DENIED','use case/zone not approved',now);if(!request.region||!policy.regions?.includes(request.region))return deny('REGION_DENIED','explicit approved region required',now);if(provider.avv_status!=='approved'||provider.brao_43e_status!=='approved'||provider.subprocessor_status!=='approved')return deny('LEGAL_VENDOR_GAP','AVV/§43e/subprocessor approval incomplete',now);if(provider.third_country===true&&provider.transfer_safeguards_status!=='approved')return deny('TRANSFER_GAP','third-country safeguards missing',now);const retention=request.retention_minutes??0;if(!Number.isInteger(retention)||retention<0||!Number.isInteger(policy.max_retention_minutes)||retention>policy.max_retention_minutes)return deny('RETENTION_DENIED','retention exceeds policy',now);if(!request.payload||typeof request.payload!=='object'||Array.isArray(request.payload))return deny('PAYLOAD_DENIED','structured payload required',now);const topFields=Object.keys(request.payload);if(!Array.isArray(policy.allowed_fields)||topFields.some(k=>!policy.allowed_fields.includes(k)))return deny('FIELD_DENIED','payload field outside signed use-case policy',now);const dlp=inspectPayload(request.payload,{allow_sensitive_keys:policy.allowed_sensitive_keys||[],max_payload_bytes:policy.max_payload_bytes||1024*1024});if(!dlp.safe)return deny('DLP_DENIED','payload contains identifier/secret or violates parser limits',now,{finding_types:[...new Set(dlp.findings.map(f=>f.type))]});const network=evaluateNetworkEgress({endpoint:request.endpoint,provider,use_case:request.use_case,region:request.region,network_probe,key_store,now});if(!network.allowed)return deny('NETWORK_DENIED',network.reason,now);return{allowed:true,code:'ALLOW',decision_id:crypto.randomUUID(),decided_at:now.toISOString(),actor_id:identity.principal.subject,tenant_id:request.tenant_id,matter_id:request.matter_id||null,provider_id:request.provider_id,use_case:request.use_case,region:request.region,endpoint:network.endpoint,payload_fingerprint:dlp.fingerprint,payload_bytes:dlp.bytes,identity_signer:identity.signer_key_id,provider_signer:providerV.signer_key_id,egress_enforcement_signer:enforced.signer_key_id,network_attestor:network.network_proof.attestor_key_id,peer_fingerprint:network.network_proof.peer_fingerprint}}

const HASH=/^sha256:[0-9a-f]{64}$/;const OPAQUE=/^[A-Za-z0-9._:-]{1,128}$/
const ACTION_SCHEMAS={send_email:{required:['message_id','recipient_refs','subject_hash','body_hash','resource_version'],arrays:['recipient_refs'],hashes:['subject_hash','body_hash']},bea_send:{required:['message_id','recipient_refs','document_hashes','resource_version'],arrays:['recipient_refs','document_hashes'],hash_arrays:['document_hashes']},case_write:{required:['record_id','mutation','value_hash','resource_version'],hashes:['value_hash'],enum:{mutation:['set_note','set_status','attach_document']}},deadline_write:{required:['deadline_id','operation','deadline_hash','resource_version'],hashes:['deadline_hash'],enum:{operation:['create','update','cancel']}},complete_task:{required:['task_id','resource_version']}}
export function validateActionPayload(action,payload){
  const s=ACTION_SCHEMAS[action]
  if(!s||!payload||typeof payload!=='object'||Array.isArray(payload))return{valid:false,reason:'unsupported action or invalid payload'}
  const allowed=new Set(s.required),keys=Object.keys(payload)
  if(s.required.some(k=>!(k in payload)))return{valid:false,reason:'required action field missing'}
  if(keys.some(k=>!allowed.has(k)))return{valid:false,reason:'unexpected action field'}
  for(const k of s.arrays||[])if(!Array.isArray(payload[k])||payload[k].length===0||payload[k].length>20)return{valid:false,reason:`${k} must be a non-empty bounded array`}
  for(const k of s.hashes||[])if(!HASH.test(payload[k]))return{valid:false,reason:`${k} must be sha256`}
  for(const k of s.hash_arrays||[])if(payload[k].some(v=>!HASH.test(v)))return{valid:false,reason:`${k} must contain sha256 values`}
  for(const [k,values] of Object.entries(s.enum||{}))if(!values.includes(payload[k]))return{valid:false,reason:`${k} value denied`}
  for(const [k,v] of Object.entries(payload)){
    if(k.endsWith('_id')||k==='resource_version'||k.endsWith('_refs')){
      if(Array.isArray(v)){if(v.some(x=>typeof x!=='string'||!OPAQUE.test(x)||x.includes('@')))return{valid:false,reason:`${k} contains unsafe reference`}}
      else if(typeof v!=='string'||!OPAQUE.test(v)||v.includes('@'))return{valid:false,reason:`${k} must be opaque`}
    }
  }
  return{valid:true,payload_hash:`sha256:${sha256(payload)}`}
}

export function issueActionApproval({identity_assertion,key_store,tenant_id,matter_id,action,payload,policy_version,expires_at,private_key,key_id,now=new Date()}){const id=verifyIdentityAssertion({assertion:identity_assertion,key_store,now});if(!id.valid||id.principal.tenant_id!==tenant_id||id.principal.mfa!==true)throw new Error('verified MFA identity required');if(!policy_version)throw new Error('active policy version required');const authAge=now.getTime()-parseTime(id.principal.auth_time);if(authAge<0||authAge>10*60*1000)throw new Error('recent step-up authentication required');const matter=authorizeMatter({principal:id.principal,tenant_id,matter_id,operation:'approve_action',zone:'MANDATE'});if(!matter.allowed)throw new Error(matter.reason);const schema=validateActionPayload(action,payload);if(!schema.valid)throw new Error(schema.reason);const expiry=parseTime(expires_at);if(expiry<=now.getTime()||expiry-now.getTime()>5*60*1000)throw new Error('approval lifetime must be >0 and <=5 minutes');const body={schema:'trustready-action-approval-v2',capability_id:crypto.randomUUID(),actor_id:id.principal.subject,actor_session_id:id.principal.session_id,tenant_id,matter_id,action,payload_hash:schema.payload_hash,resource_version:payload.resource_version,policy_version,issued_at:now.toISOString(),expires_at,nonce:crypto.randomBytes(32).toString('hex')};return signEnvelope({body,private_key,key_id,purpose:'action_approval'})}
export async function executeApprovedAction({capability,identity_assertion,key_store,tenant_id,matter_id,action,payload,policy_version,runtime_state,deployment_mode='shadow',replay_store,action_handler,now=new Date()}){const state=evaluateRuntimeState({state:runtime_state,tenant_id,provider_id:'__actions__',action:true});if(!state.allowed)return{executed:false,code:'KILL_SWITCH',reason:state.reason};if(!policy_version||runtime_state.policy_version!==policy_version)return{executed:false,code:'POLICY_VERSION_DENIED',reason:'active policy version mismatch'};if(deployment_mode==='shadow'&&HIGH_RISK_ACTIONS.has(action))return{executed:false,code:'SHADOW_LOCK',reason:'high-risk action disabled in shadow mode'};const id=verifyIdentityAssertion({assertion:identity_assertion,key_store,now});if(!id.valid||id.principal.tenant_id!==tenant_id)return{executed:false,code:'IDENTITY_DENIED',reason:id.reason||'tenant mismatch'};const matter=authorizeMatter({principal:id.principal,tenant_id,matter_id,operation:'execute_action',zone:'MANDATE'});if(!matter.allowed)return{executed:false,code:'MATTER_DENIED',reason:matter.reason};const schema=validateActionPayload(action,payload);if(!schema.valid)return{executed:false,code:'ACTION_SCHEMA_DENIED',reason:schema.reason};const v=verifyEnvelope({envelope:capability,key_store,purpose:'action_approval',now});if(!v.valid)return{executed:false,code:'APPROVAL_DENIED',reason:v.reason};const b=v.body;if(b.schema!=='trustready-action-approval-v2'||parseTime(b.expires_at)<=now.getTime())return{executed:false,code:'APPROVAL_DENIED',reason:'approval expired/unsupported'};const expected={actor_id:id.principal.subject,actor_session_id:id.principal.session_id,tenant_id,matter_id,action,payload_hash:schema.payload_hash,resource_version:payload.resource_version,policy_version};for(const[k,val]of Object.entries(expected))if(b[k]!==val)return{executed:false,code:'APPROVAL_MISMATCH',reason:`${k} mismatch`};if(!replay_store||replay_store.durable!==true||replay_store.atomic!==true||typeof replay_store.claim!=='function')return{executed:false,code:'REPLAY_STORE_REQUIRED',reason:'durable atomic replay store required'};const claimed=await replay_store.claim({idempotency_key:b.capability_id,nonce:b.nonce,expires_at:b.expires_at});if(claimed!==true)return{executed:false,code:'REPLAY_DENIED',reason:'capability already consumed or replay store unavailable'};if(typeof action_handler!=='function')return{executed:false,code:'HANDLER_REQUIRED',reason:'typed action handler required; capability consumed fail-safe'};const result=await action_handler({actor:id.principal.subject,tenant_id,matter_id,action,payload,capability_id:b.capability_id});return{executed:true,code:'EXECUTED',capability_id:b.capability_id,result}}
